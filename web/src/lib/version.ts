/**
 * Versão e atribuição do app: usadas no rodapé discreto da sidebar.
 *
 * Esquema de versão (definido em 2026-06-02, alinhado com releaseNotes.ts):
 *   - patch (1.2.X) → CADA atualização incremental. É o caso normal: a cada
 *     deploy sobe só o último numeral (1.2.1, 1.2.2 … pode ir até 1.2.65).
 *   - minor (1.X.0) → MARCO grande (ex.: 1.2.0 = nova identidade visual).
 *   - major (X.0.0) → quebra contrato/dados (raro).
 *
 * `APP_ATTRIBUTION` é o crédito sutil no canto da sidebar.
 *
 */
export const APP_VERSION = '1.3.25'
/** Data de lançamento da versão atual (ISO yyyy-mm-dd). Atualizar junto com APP_VERSION. */
export const APP_VERSION_DATE = '2026-08-14'
export const APP_ATTRIBUTION = 'Validator Demo · dados fictícios'
